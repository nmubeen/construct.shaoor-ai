import { notFound,redirect } from "next/navigation"; import { ProjectForm } from "@/components/dashboard/projects/ProjectForm"; import { requireActiveConstructContext } from "@/lib/auth/construct-context"; import { getConstructPrisma } from "@/lib/construct-prisma";
export default async function EditProjectPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const c=await requireActiveConstructContext();if(c.role==="VIEWER")redirect("/dashboard/projects");const{id}=await params;const project=await getConstructPrisma().project.findFirst({where:{id,organizationId:c.organizationId},include:{highlights:true,galleryItems:true}});if(!project)notFound();const{error}=await searchParams;return <div className="px-4 py-6 sm:px-6 lg:px-8 lg:py-8"><h1 className="mb-6 text-3xl font-bold">Edit project</h1><ProjectForm project={project} error={error}/></div>}
